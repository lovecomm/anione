doctype html
html(lang="en")
	head
		title= "Anione Development"
		meta(charset='UTF-8')
		style(type="text/css").
			body {
				background-color: #cdcdcd;
				margin: 0;
			}
	body
		div#nav
			each banner in banners
				| <div><button data-link="#{banner.path}">#{banner.layerName}</button></div>		
		div#view
			